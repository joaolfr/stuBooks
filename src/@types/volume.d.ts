export declare global {
  type VolumeType = {
    title: string
    description: string
    authors: string[]
    publisher: string
    publishedDate: string
    imageLinks: {
      thumbnail: string
    }
  }
}
